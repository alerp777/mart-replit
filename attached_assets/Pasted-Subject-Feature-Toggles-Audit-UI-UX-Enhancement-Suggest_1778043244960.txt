Subject: Feature Toggles Audit & UI/UX Enhancement Suggestions – AJKMart"Hi,I have configured the Services & Features toggles in the Admin Panel. Since these are 'API Enforced', I need a full technical audit to ensure the 'Kill Switch' logic is working perfectly. Please execute the following:1. Technical API Audit503 Error Handling: When a service (like 'Rides' or 'Food') is toggled OFF, verify that the API returns a clean 503 Service Unavailable error and the frontend shows a user-friendly 'Service Temporarily Offline' message instead of a crash.MPIN & Wallet Security: Verify that when MPIN Enforcement is ON, the backend strictly rejects any wallet transaction request that doesn't include a valid MPIN header.New User Gating: Confirm that New User Registration toggle effectively blocks the auth/register endpoint while allowing existing sessions to remain active.2. Design & UI Recommendations (For Enhancing User Experience)While auditing, please consider implementing these UI improvements:Dynamic Home Screen: If a service (e.g., Pharmacy) is toggled OFF, its icon should either be greyed out with a 'Coming Soon' badge or hidden completely to avoid user frustration.Weather Widget Optimization: Since the Weather Widget is 'Client-Side', ensure it uses the user's current GPS rather than a fixed city to make it more personalized for people traveling within AJK.SOS Visibility: The SOS button should only appear during an active trip. Design-wise, it should be high-contrast (Bright Red) but placed where it won't be clicked accidentally.MPIN Workflow: For the MPIN Enforcement, please use a 'Secure Entry' UI (masked dots) and ensure the keyboard defaults to a numeric keypad for a faster experience.Skeleton Loaders: While the API checks for feature status, use skeleton loaders on the home screen so the UI doesn't 'jump' when a service is hidden/shown.3. Live Tracking ReliabilityPlease stress-test the Live GPS Order Tracking. Ensure the 'Rider' app updates the coordinates at least every 5-10 seconds so the 'Customer' sees a smooth movement on the map, not a jumping icon.Please let me know once the API gates are verified and if we can schedule these UI enhancements in the next sprint.